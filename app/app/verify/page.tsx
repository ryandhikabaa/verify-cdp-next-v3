import {VerifyWorkspace} from '@/components/verifier/VerifyWorkspace';

/** Hosts the main application verifier page under the app area. */
export default function AppVerifyPage() {
  return <VerifyWorkspace />;
}
